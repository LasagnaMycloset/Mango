# Mango

-- Mango is basically a homemade Git 

== In the main file there is a variable called (DB_PATH) for it make a new file containing {} with no quotation marks and set the variable the new file's path
